{
  "framework": null,
  "buildCommand": "npx nx build twenty-shared --parallel=1 --skip-nx-cache && npx nx build twenty-front --parallel=1 --skip-nx-cache && cp -r packages/twenty-front/build ./build",
  "outputDirectory": "build",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
